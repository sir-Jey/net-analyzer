Пассивный анализатор сетевого трафика.

Что умеет

- Перехватывает TCP, UDP, ICMP пакеты
- Для TCP: определяет операционную систему по TTL, Window Size и опциям (MSS, SACK, Timestamp, Window Scale), вычисляет расстояние в хопах, показывает DF и IP ID
- Для UDP и ICMP: выводит базовую информацию (адреса, порты, длину, тип ICMP-сообщения)
- Фильтрует трафик по протоколу и порту (через BPF)
- Завершается после заданного количества SYN-пакетов или по Ctrl+C с выводом статистики

Зависимости

- libpcap (dev)
- gcc (или clang)

Установка

MacOS

```bash
brew install libpcap
```

### Linux (Ubuntu/Debian)

```bash
sudo apt-get install libpcap-dev
```

## Компиляция и запуск

```bash
gcc -o net-analyzer main.c -lpcap
sudo ./net-analyzer
```

## Опции

| Опция | Действие |
|-------|----------|
| `-i <интерфейс>` | Интерфейс для прослушки (по умолчанию: выбирается первый) |
| `-c <количество>` | Завершиться после N SYN-пакетов (по умолчанию: бесконечно) |
| `-p <tcp/udp/icmp>` | Фильтр по протоколу (по умолчанию: tcp) |
| `--port <номер>` | Фильтр по порту (для tcp и udp) |
| `-h` | Справка |

## Примеры

```bash
# Запуск с фильтром по протоколу и порту
sudo ./net-analyzer -p udp --port 53
sudo ./net-analyzer -p icmp
```

## Пример вывода

```
192.168.0.4:54321 -> 93.184.216.34:80 | расстояние=7 роутеров | df=1 id=54321 | предполагаемая ОС=Linux | win=65535 | mss=1460 sack=yes wscale=8 tsval=12345678 tsecr=0
```

## Остановка

Ctrl+C — завершение с выводом статистики.

Понял. Просто добавь в README.md раздел про `synsend`.

Вот готовый текст, который допиши в конец README (перед "Лицензия" или перед "Автор"):

---

```markdown
## Тестовый инструмент

В папке `tools/` находится `synsend` — утилита для отправки SYN-пакета. Используется для проверки работоспособности сниффера.

### Компиляция synsend

```bash
cd tools
gcc -o synsend synsend.c -lnet
```

### Зависимости для synsend

- libnet (`brew install libnet` на macOS, `sudo apt-get install libnet-dev` на Linux)

### Пример проверки сниффера

1. Запустить сниффер: `sudo ./net-analyzer`
2. В другом терминале отправить тестовый SYN: `sudo ./tools/synsend 93.184.216.34 80`
3. Сниффер должен показать отправленный пакет.

> Примечание: На macOS отправка сырых пакетов может быть ограничена системой. Для полноценной работы synsend рекомендуется использовать Linux.
```

---

Ты можешь просто скопировать этот блок и вставить в свой README.md.
